# REGULR.vip LOYALTY PLATFORM - KILOCODE PROJECT SPEC
# Technisch ontwerp & implementatie handleiding

## ROLLEN & RECHTEN SYSTEEM

### 1. GEBRUIKERSROLLEN
Platform: REGULR.vip Loyalty Platform
Versie: 1.0.0

## DOELSTELLING
Deze KILOCODE.md beschrijft de technische architectuur en implementatie details van het REGULR.vip platform. De inhoud is gericht op implementatie en beheer.

## INHOUD
1. Project structuur
2. Gebruikersrollen en toegangsrechten
3. Superadmin = Platform beheerder
4. Admin = Manager (kroegbaas/restaurant eigenaar)

## MODULE OVERZICHT
- Superadmin: Platform beheerder (kan alle tenants beheren)
- Admin: Kroegbaas/restaurant eigenaar

## TOEGANGSTRAJECT
1. Superadmin: Platform beheerder
2. Admin: Kroegbaas/restaurant eigenaar

## EMAIL SYSTEEM TECHNISCHE SPECIFICATIES

### 1. DATABASE STRUCTUUR
Het emailsysteem maakt gebruik van drie database tabellen:

#### 1.1 email_config
Bevat de SMTP configuratie instellingen:
- id (INT, AUTO_INCREMENT, PK)
- provider (ENUM: 'brevo', 'sender_net', 'aws_ses')
- smtp_host (VARCHAR)
- smtp_port (INT)
- smtp_user (TEXT, versleuteld)
- smtp_pass (TEXT, versleuteld)
- from_email (VARCHAR)
- from_name (VARCHAR)
- is_active (BOOLEAN)
- created_at (TIMESTAMP)
- updated_at (TIMESTAMP)

#### 1.2 email_templates
Bevat de email template configuratie:
- id (INT, AUTO_INCREMENT, PK)
- type (ENUM: 'tenant_welcome', 'admin_invite', 'guest_confirmation', 'guest_password_reset', 'marketing')
- subject (VARCHAR)
- content (TEXT)
- text_content (TEXT, optioneel)
- tenant_id (INT, FK naar tenants.id)
- language_code (VARCHAR)
- is_default (BOOLEAN)
- created_at (TIMESTAMP)
- updated_at (TIMESTAMP)

#### 1.3 email_log
Bevat de email verzend log:
- id (INT, AUTO_INCREMENT, PK)
- tenant_id (INT)
- user_id (INT, optioneel)
- recipient_email (VARCHAR)
- subject (VARCHAR)
- template_type (VARCHAR)
- template_id (INT)
- provider (VARCHAR)
- status (ENUM: 'pending', 'sent', 'failed')
- error_message (TEXT)
- sent_at (TIMESTAMP)
- created_at (TIMESTAMP)

### 2. EMAIL TEMPLATE SYSTEEM

#### 2.1 Template Types
Er zijn vijf template types beschikbaar:
1. tenant_welcome - Voor nieuwe tenant aanmeldingen
2. admin_invite - Voor admin uitnodigingen
3. guest_confirmation - Voor gast registratie bevestigingen
4. guest_password_reset - Voor gast wachtwoord reset
5. marketing - Voor marketing campagnes

#### 2.2 Template Variabelen
Templates maken gebruik van Mustache-achtige placeholders:
- {{tenant_name}} - Tenant naam
- {{user_name}} - Gebruikersnaam
- {{password_reset_link}} - Wachtwoord reset link
- {{invitation_link}} - Uitnodigingslink
- {{verification_code}} - Verificatie code
- {{campaign_name}} - Campagne naam
- {{campaign_message}} - Campagne bericht
- {{action_url}} - Actie URL
- {{action_text}} - Actie tekst
- {{unsubscribe_url}} - Afmeld URL

### 3. TOEGANGSRECHTEN
- Superadmin: Beheert tenant_welcome templates
- Admin: Beheert admin_invite, guest_confirmation, marketing templates
- Gast: Geen template beheer, alleen ontvangen

### 4. EMAIL PROVIDERS
Ondersteunde email providers:
1. BREVO (standaard)
2. Sender.net
3. AWS SES

Elke provider kan worden geconfigureerd via de superadmin interface.

## GATED ONBOARDING (KYC-LIGHT) — ACCOUNT STATUS SYSTEEM

### STATE MACHINE
```
unverified ──(barman valideert ID)──> active
unverified ──(barman weigert)──────> unverified (met cooldown)
active ──────(admin blokkeert)──────> suspended
suspended ───(admin deblokkeert)────> active
```

### GAST RECHTEN PER ACCOUNT STATUS

| Actie | unverified | active | suspended |
|-------|:----------:|:------:|:---------:|
| Inloggen | ✅ | ✅ | ❌ |
| Dashboard bekijken | ✅ | ✅ | ❌ |
| QR code tonen | ✅ (met "UNVERIFIED" badge) | ✅ | ❌ |
| Saldo storten | ❌ | ✅ | ❌ |
| Betalen aan bar | ❌ | ✅ | ❌ |

### TOEGANGSRECHTEN PER ROL
- Superadmin: Platform beheerder (geen account_status restricties)
- Admin: Kan gasten blokkeren (`suspend`) en deblokkeren (`unsuspend`) via `/api/admin/suspend_user`
- Bartender: Kan unverified gasten verifieren via `/api/pos/verify` (met birthdate match)
- Gast unverified: Kan inloggen, dashboard bekijken, QR tonen. Kan NIET storten of betalen.
- Gast active: Volledige toegang (bestaand gedrag).
- Gast suspended: Kan niet inloggen (login wordt stil geblokkeerd, geen info leakage).

### RATE LIMITING (configureerbaar per tenant)
- `verification_soft_limit` (default 15): Waarschuwing per barman per uur
- `verification_hard_limit` (default 30): Absolute blokkade per barman per uur
- `verification_cooldown_sec` (default 180): Seconden wachttijd na mismatch
- `verification_max_attempts` (default 2): Max verificatiepogingen per gast per 24u

### VERIFICATION TOGGLE (configureerbaar per tenant, superadmin-only)

Elke tenant heeft een `verification_required` (BOOLEAN, default `1`) kolom in de `tenants` tabel.

- **Aan (`1`, default)**: Gasten starten als `unverified` en moeten fysiek geverifieerd worden door een barman (KYC-light flow).
- **Uit (`0`)**: Gasten krijgen direct `account_status = 'active'` bij registratie. Geen barman-verificatie nodig.

| Instelling | Nieuwe gast | Bestaande unverified | POS scan | Deposit | Betaling |
|:-----------:|:-----------:|:--------------------:|:--------:|:-------:|:--------:|
| Aan (1)     | `unverified` | Onveranderd          | Verify panel | ❌ | ❌ |
| Uit (0)     | `active`     | Onveranderd          | Direct payment | ✅ | ✅ |

**Belangrijk**: De toggle is NIET retroactief. Bestaande `unverified` gasten blijven `unverified` na uitschakelen. `suspended` accounts blijven altijd geblokkeerd, ongeacht de toggle.

#### Toegangsrechten
- Superadmin: Kan `verification_required` per tenant in-/uitschakelen via `/api/superadmin/tenants`
- Admin: Kan de toggle NIET wijzigen (read-only zichtbaar in instellingen, gelijk aan `feature_push`/`feature_marketing`)
- Bartender: Verify panel wordt overgeslagen als toggle uit staat; `/api/pos/verify` retourneert error
- Gast: Banners/gating worden niet getoond als toggle uit staat

### RELEVANTE BESTANDEN
- `sql/gated_onboarding_migration.sql` — Database migratie (account_status)
- `sql/verification_toggle_migration.sql` — Database migratie (verification_required toggle)
- `api/pos/verify.php` — Barman verificatie endpoint
- `api/admin/suspend_user.php` — Admin blokkeren/deblokkeren
- `models/User.php` — account_status methodes + verification_required in create()
- `services/AuthService.php` — register() leest tenant toggle
- `views/bartender/dashboard.php` — Verify state panel (conditional)
- `views/superadmin/tenant_detail.php` — Toggle switch
- Plan: `.kilo/plans/gated_onboarding_implementation.md`
- Plan: `.kilo/plans/verification_toggle.md`